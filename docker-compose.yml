version: '3.8'

services:
  app:
    build: 
      context: .
      args:
        - VITE_APP_URL=${VITE_APP_URL}
    restart: always
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - VITE_APP_URL=${VITE_APP_URL}
    networks:
      - omnidash-net

  caddy:
    image: caddy:latest
    restart: always
    environment:
      - VITE_APP_URL=${VITE_APP_URL}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - omnidash-net
    depends_on:
      - app

networks:
  omnidash-net:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
